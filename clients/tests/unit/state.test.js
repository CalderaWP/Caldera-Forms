//import rootReducer from '../../privacy/reducers/index';

describe( 'privacyRootRecuser', () => {
   it( 'Has 3 reducers combined', () => {
       expect(Object.keys({a:1,b:1,c:1}).length).toBe(3);
   })
});